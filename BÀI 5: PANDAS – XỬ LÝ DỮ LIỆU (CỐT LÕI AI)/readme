1️⃣ Cài Pandas
pip install pandas

2️⃣ Tạo DataFrame (bảng dữ liệu)
import pandas as pd

data = {
    "name": ["An", "Bình", "Duy"],
    "age": [20, 21, 22],
    "score": [8.5, 7.0, 9.0]
}

df = pd.DataFrame(data)
print(df)

3️⃣ Xem dữ liệu (AI luôn làm bước này)
print(df.head())     # 5 dòng đầu
print(df.info())     # cấu trúc dữ liệu
print(df.describe()) # thống kê

4️⃣ Truy cập cột & dòng
print(df["age"])
print(df.loc[0])     # dòng đầu

5️⃣ Lọc dữ liệu (rất hay dùng)
# Sinh viên có điểm >= 8
gioi = df[df["score"] >= 8]
print(gioi)

6️⃣ Thêm cột mới (feature engineering)
df["passed"] = df["score"] >= 5
print(df)


👉 Đây chính là tạo đặc trưng cho AI

7️⃣ Đọc dữ liệu từ file CSV
df = pd.read_csv("data.csv")
