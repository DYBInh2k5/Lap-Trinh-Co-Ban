# List & vòng lặp (xử lý dữ liệu)

numbers = [1, 2, 3, 4, 5]

for n in numbers:

    print(n * 2)


AI làm việc chủ yếu với danh sách dữ liệu

# List nâng cao

nums = [1, 2, 3, 4, 5]

Lấy số chẵn

even_nums = []

for n in nums:

    if n % 2 == 0:

        even_nums.append(n)

print(even_nums)

# Hàm – tư duy cực quan trọng trong AI

def square(x):

    return x * x

print(square(5))
